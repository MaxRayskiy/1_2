sudo: required

language: cpp

os:
  - linux

compiler:
  - gcc

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test

script:
  - cmake CMakeLists.txt
  - make ./executeTests
  - ./executeTests


notifications:
  email: true